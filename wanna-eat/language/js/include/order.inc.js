export { Order };

class Order {
    // getOrders(orderId = '', callback) {
    //     const submitOrder = new AjaxData(`group_buy_api.php?res=order_list&order_id=${orderId}`, callback);
    //     submitOrder.get()
    // }
}